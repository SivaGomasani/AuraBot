def check_login(username, password):
    # Replace with your own login logic, e.g., check against a database
    if username == 'admin' and password == 'password':
        return True
    return False
